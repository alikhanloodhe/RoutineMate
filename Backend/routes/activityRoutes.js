// routes/activityRoutes.js


// const router = express.Router();
// router.use(authenticate);
// router.post('/addActivity',  upload.single('photo'), addActivityWithPhoto);

// export default router;
